{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "A portfolio for Joph",
  "repository": "stop warning",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "metalsmith": "^1.0.1"
  }
}
