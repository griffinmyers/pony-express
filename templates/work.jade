extends ./layout.jade

block vars
  - var title = locals.title

block content
  .row.work
    .one-half.column.u-pull-right.work-header
      != contents
  .row.work
    .one-half.column.work-assets
      != left
    .one-half.column
      != right